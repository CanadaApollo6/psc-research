# Data provenance

Snapshot: 2026-09-11. The exact URLs, output filenames, byte limits and SHA-256 hashes are recorded in [config/sources.json](../config/sources.json).

The later [QTL follow-up manifest](../config/qtl-followup-sources.json) adds 39 pinned public sources; [its query manifest](../config/qtl-followup-queries.json) records eight indexed extractions and all 121 retained rows. Source metadata uses eQTL Catalogue release 7 from resources commit `65995fe96c8c6da1e750b51bac3b4c2f5ba9260f`; the queried archive files are pinned through their headers, indexes and exact subsets, without claiming a hash of the full remote files. The [source report](../reports/qtl-followup.md) distinguishes reprocessed BLUEPRINT, DICE, Lepik and TwinsUK cohorts, their RNA endpoints and missing measurements. The Catalogue API is retired; its official [archive access page](https://www.ebi.ac.uk/eqtl/Data_access/) documents indexed queries and ALT effect-allele semantics.

LeafCutter phenotype metadata comes from [Zenodo record 7850746](https://zenodo.org/records/7850746), Ensembl 105. The conversion to model junction intervals is explicit, and conflicting cluster/gene strands are preserved separately. The [AlphaGenome training-track extract](../data/derived/qtl-comparison-training-tracks.csv) comes from published Supplementary Table 2, not an inferred inventory of the live service. Raw papers, metadata workbooks and compressed indexes stay ignored; small derived tables retain source attribution.

The [direct-junction manifest](../config/ubash3a-junction-sources.json) pins 35 sources for the next analysis: two exact Snaptron junction responses, recount3/ENA study metadata, direct sample-ID responses, public 1000 Genomes metadata/index, reference DNA, workflow code and data-access evidence. The [run manifest](../config/ubash3a-junction-run.json) separately pins the one-variant VCF subset and the plan saved before target counts were inspected. The VCF's missing rsID is preserved; exact GRCh37 coordinates and A/C alleles match the previously verified mapping. All 667 RNA runs are matched explicitly and combined into 464 donor records, of which 447 have public genotypes. Public sample-level joins remain ignored; only aggregate results are versioned. [Full methods and limitations](../reports/ubash3a-junction-followup.md).

## Imported inputs

| Input | Source | Transformation |
|---|---|---|
| Article XML, PMC11541731 | [Europe PMC full-text XML](https://www.ebi.ac.uk/europepmc/webservices/rest/PMC11541731/fullTextXML) | Extract Table 1 and expand merged cells so secondary signals keep the correct locus. |
| Supplementary Data 1–3 | [Goode et al. article attachments](https://www.nature.com/articles/s41467-024-53602-w) | Cache all three small files; import Data 2's colocalisation evidence. Data 1 and 3 are supporting source material. |
| GSE243977 SOFT metadata | [GEO](https://www.ncbi.nlm.nih.gov/geo/query/acc.cgi?acc=GSE243977) | Decompress; extract sample fields; derive explicitly provisional title labels and flag selected cross-field differences. |
| GSE247128 SOFT metadata | [GEO](https://www.ncbi.nlm.nih.gov/geo/query/acc.cgi?acc=GSE247128) | Same metadata-only transformation. |

Hashes apply to the exact files stored in `data/raw/`, **after decompression** for SOFT files. Downloads are bounded in size and fail on a changed hash. Raw files and the run-specific retrieval log are ignored by Git. Derived outputs can be regenerated with the documented commands.

## Attribution and reuse

Goode EC, Fachal L, Panousis N, et al. *Fine-mapping and molecular characterisation of primary sclerosing cholangitis genetic risk loci*. Nature Communications 15, 9594 (2024). DOI: [10.1038/s41467-024-53602-w](https://doi.org/10.1038/s41467-024-53602-w). The article is licensed [CC BY 4.0](https://creativecommons.org/licenses/by/4.0/). Derived CSVs change the format and expand merged cells; source content and attribution are retained. Third-party material can have separate terms.

The GEO records originate from the [PSC liver-atlas study](https://pubmed.ncbi.nlm.nih.gov/38199298/) and its submitting investigators. Accession and sample links remain attached to the derived rows. Public access does not grant unrestricted rights to every linked dataset; check each source's terms before reusing additional material.

Individual-level genotype and expression files at EGA can require controlled access. They were not downloaded or required for the initial import. A later analysis uses public liver expression objects, described below, cached outside Git. Personal genomes and private medical records are not included.

## Variant-reference and prediction provenance

[config/reference-sources.json](../config/reference-sources.json) pins 27 additional responses. Ensembl release 116 provides variant mappings on both builds, individual reference bases, and genes overlapping fixed 1-Mb neighborhoods. Build-38 reference bases were also retrieved directly from AlphaGenome's GRCh38.p13 FASTA using its `.fai` index and exact HTTP byte ranges. Both the index and the one-base responses have recorded hashes. All alternative mappings remain in raw inputs; ambiguous primary mappings are rejected by the normalizer.

The variant table distinguishes published risk labels from reference/alternate alleles. Candidate-gene tables are derived from the pinned gene annotations. The [run provenance](../reports/pilot-run-provenance.json) records client commit, selected model version, protocol/input hashes, metadata hash, request dates and prediction-file hashes. The [pilot report](../reports/first-alphagenome-pilot.md) includes compact model-derived results; full output tables remain in ignored local storage.

AlphaGenome access and outputs have [separate terms documented by the official project](https://github.com/google-deepmind/alphagenome#terms). The current work is noncommercial research. Do not assume a future code license would override source-data or model-output conditions.

## Mechanism follow-up sources

[mechanism-sources.json](../config/mechanism-sources.json) pins article XML/HTML, the 2020 Goode thesis, GWAS Catalog study/association metadata, and Ensembl transcript/variant context. Where Europe PMC did not expose XML, the public PMC HTML was used. No browser challenge was treated as article content. The thesis's relevant allele passages on pages 60 and 80 were rendered and visually checked.

[association-sources.json](../config/association-sources.json) pins the original GWAS file header, directory metadata, 23 small byte ranges and rs313839 population frequencies. `fetch_gwas_audit_rows.py` verifies sorted complete interior lines, skips partial boundary fragments and requires an exact rsID/coordinate match. The two selected lines are copied without changing their original fields. `build_allele_audit.py` verifies those copies against pinned ranges and derives separate reviewed fields. The 662,022,204-byte file is not downloaded in full. Its header defines `allele_1` as risk; sample scope and conflicting source labels are described in the audit.

[splice-audit-sources.json](../config/splice-audit-sources.json) pins the official model's coordinate implementation to research-code commit `0db53bd4352c66d1e00a049a81da373a066e6670` and the 2020 Mucaki splicing paper. This code was read to confirm coordinates; it was not used to run a different model. The API client remains pinned separately.

The one additional model request has [separate provenance](../reports/splice-followup-provenance.json). Its original v0.1 protocol is archived unchanged; v0.2 records the post-request donor-index correction. Complete output arrays, native junction coordinates and metadata are saved locally with hashes. Summary joins use feature coordinates and assay identities rather than row order; unreturned features remain missing. The downstream donor is the largest positive change within the originally fixed 101-base window, selected for exploratory interpretation. It is not an independently chosen validation endpoint.

## Full fine-mapping archive and prospective comparison

The September 12 [fine-mapping manifest](../config/finemap-sources.json) pins 43 public sources, including the 16,716,205-byte archive from [Zenodo 11143561](https://zenodo.org/records/11143561), its record metadata, FINEMAP and AlphaGenome documentation, nine rsIDs' mappings on both Ensembl builds, six substitutions' reference bases on both builds and the model FASTA, and reused release/index metadata. The archive's publisher MD5 is `35319e93d376e80958b3c78ae92c1fe5`; its SHA-256 is recorded in the manifest. Zenodo identifies its license as CC BY 4.0. Attribution is to Goode et al. (2024); the new tables change format and add clearly separate audit calculations. Reference services and documentation retain their own terms.

`audit_finemap_archive.py` reads every regular archive member without extracting paths, checks all 26 SNP/configuration/log triplets, and preserves all 71,083 variant rows in a deterministic gzip CSV. Its configuration sums are separate from the original four-decimal PIPs. It compares the saved posterior distribution over causal counts with the logs and reconstructs cumulative sets only for complete singleton-only configurations. The [report](../reports/psc-finemap-and-comparison.md) documents the discrepancies and missing original inputs; the resulting tables are not complete signal-specific credible sets or normalized genotype inputs.

`prepare_controlled_comparison.py` applies the [fixed protocol](../config/psc-controlled-comparison.json), checks the six unambiguous substitutions against the pinned Ensembl 116 and GRCh38.p13 references, and accounts for all 12 high-PIP source records at the selected regions. Five are nominated for matching, one is a reserve and six are excluded. All risk directions remain unspecified. The [freeze record](../config/psc-controlled-comparison-lock.json) pins the protocol and current inputs; future comparator data should use a separate source manifest. Frequencies, LD, matched comparator identities and the common gene/track universe have not yet been prepared. No new model inference or researcher contact occurred in this phase.

## Completed comparator preparation and model run

The later [comparison manifest](../config/psc-comparator-sources.json) adds 85 pinned response records without changing the preceding 43-source manifest. It records complete request bodies and retrieval times for 64 Ensembl bulk-variation requests covering all 6,117 low-PIP rsIDs on both GRCh37 and GRCh38, plus Ensembl 116 gene annotations/release metadata, population indexes/panel, reference sequences and model annotation/implementation. The [Ensembl API](https://rest.ensembl.org/documentation/info/variation_post) documents the 200-ID batch limit. Canonical replacements under a different response key are not silently joined to the original source rsID; raw responses preserve them for future explicit alias review. The mapping audit distinguishes all exclusions.

The [1000 Genomes phase 3 collection](https://www.internationalgenome.org/data-portal/data-collection/phase3/) provides the public release 20130502 sample panel and chromosome 2, 6 and 10 genotype archives. Three bounded indexed queries retain only exact GRCh37 coordinate/REF/ALT matches from the target list; no complete chromosome file is downloaded. The [query record](../config/psc-comparator-genotype-queries.json) pins each index, target/plan hashes, pysam version, sample-bearing VCF subset and its uncompressed content. The raw subsets contain all 2,504 sample columns; matching uses only the panel's 503 EUR donors. Genotype records are joined by coordinates, alleles and donor identities, not VCF row order or rsID alone. All 541 targets are reference-verified, and 540 have exact genotype records. Donor-level inputs remain ignored; frequencies and LD summaries are versioned.

The model [GENCODE v46 annotation](https://storage.googleapis.com/alphagenome/reference/gencode/hg38/gencode.v46.annotation.gtf.gz.feather) is 333,040,258 bytes, read as an Arrow IPC file. Its transcript TSS membership follows the source-pinned official extractor, including its negative-strand exclusive-End point query. The final gene universe intersects this coverage with Ensembl gene-level TSSs inside each fixed interval. That intersection is used for both nearest-TSS matching covariates and scoring, before predictions. Two implementation files are pinned to research-code commit `0db53bd4352c66d1e00a049a81da373a066e6670`; they were read for semantics, not executed as a replacement model.

The [RNA metadata record](../config/psc-comparator-model-metadata.json) separately pins an AlphaGenome metadata RPC response with 667 track rows. Its three `CL:0000624` rows represent plus/minus total RNA and unstranded polyA RNA; stranded merging yields two fixed primary tracks. `fetch_comparison_metadata.py` verifies the cache or retrieves metadata without making variant predictions, and rejects a changed hash. The final [input lock](../config/psc-matched-comparison-inputs.json) pins matching covariates, algorithms, windows, genes and track identities. It was saved before any of the nine requests.

The [model run provenance](../reports/matched-comparison-run-provenance.json) records request times, pinned client, `ALL_FOLDS` selection, runner hash and each prediction-file hash. The service does not expose a server model build identifier through this client. Compact results retain all 228 variant–gene pairs and 456 primary scores; full prediction tables stay ignored. The [summary record](../reports/matched-comparison-summary.json) additionally pins the reporting script. Gene-mask scores are natural-log differences with a 0.001 pseudocount, as documented by the [official implementation](https://github.com/google-deepmind/alphagenome_research/blob/0db53bd4352c66d1e00a049a81da373a066e6670/src/alphagenome_research/model/variant_scoring/gene_mask.py). Source-data and model-output terms remain separate; this work does not relicense them. See [the complete findings and limitations](../reports/matched-comparison.md).

## CD4 expression evidence for the full matched set

The [CD4 source manifest](../config/cd4-rna-sources.json) pins 36 supporting responses: release-7 study/path metadata, methods and field definitions, original DICE/BLUEPRINT/PSC papers, the model training-track supplement, ten nominal-archive indexes and ten bounded header responses, the [gene-phenotype metadata record](https://zenodo.org/records/7808390) and its 1,905,386-byte gene-count annotation, plus four live public ENCODE experiment responses. Several existing sources are reused by hash; this count does not mean 36 newly generated datasets. The metadata revision remains `65995fe96c8c6da1e750b51bac3b4c2f5ba9260f`.

The [plan](../config/cd4-rna-plan.json) fixes all 228 model-derived variant–gene pairs in ten CD4 contexts, exact allele/gene matching, missing-data rules, a 2,280-test Bonferroni screen and reporting scope before target effects. The [input lock](../config/cd4-rna-input-lock.json) pins the complete source manifest before queries. A [separate 90-query record](../config/cd4-rna-queries.json) includes the reader hash, archive/index/header identities, exact GRCh38 intervals, request times and compressed/uncompressed subset hashes. Queries use `.all.tsv.gz` nominal files, which contain upstream-tested pairs rather than only significant associations. No whole multi-gigabyte archive or donor-level RNA/genotype data is needed.

The 1,047 returned original summary rows are preserved in deterministic compressed subsets under `data/derived/cd4-rna-query-rows/`. Original rsIDs, allele fields, nominal values and rows outside the analysis universe remain available. Analysis uses exact chromosome/position/REF/ALT, with ALT as effect allele, and stable Ensembl gene IDs after removing only numeric version suffixes. Gene-level molecular-trait identity is checked. Rows differing only by rsID aliases can be collapsed; conflicting measurements are unavailable. Source sample counts, minor-allele carriers, optional imputation quality and TPM are retained. No source value is replaced with an inferred clinical risk direction.

The older Ensembl 105 gene-phenotype annotation differs from the model's newer universe. All 2,280 planned comparisons remain in [the evidence table](../reports/cd4-rna-evidence.csv), including absent annotations, absent exact variants and missing pairs. Missing measurements are not null associations. DICE QTD000464/469 sample-group naive/memory labels conflict with their tissue labels; both source fields remain in the output. All ten DICE/BLUEPRINT contexts were selected before effect inspection, without resolving that conflict by assumption.

Published training metadata and live ENCODE JSONs identify experiments ENCSR033XWU, ENCSR411MUF, ENCSR463JBR and ENCSR545MEZ. The first two share a donor; the four experiments identify three public donor accessions in total. The [training-source audit](../reports/cd4-rna-training-provenance.csv) records accession-level evidence without claiming that it establishes overlap or non-overlap with QTL donors. DICE subsets share donors, and both DICE and BLUEPRINT were included in Goode et al.'s PSC evidence review. The [report](../reports/cd4-rna-evidence.md) distinguishes these limits from molecular association strength. The [summary](../reports/cd4-rna-summary.json) pins the reporting code; previous protocols and model outputs are unchanged.

## Regional colocalisation sources and sample-count amendment

The [colocalisation source manifest](../config/coloc-sources.json) pins 82 supporting responses, including reused CD4 metadata, UCSC reciprocal build chains, the original Ji 2017 GWAS metadata/header, ten full published credible-set files and ten bounded BF header responses, and official coloc/SuSiE documentation and code. The [original plan](../config/coloc-plan.json) fixes both TSS ±1 Mb windows and all twenty gene/context comparisons. Separate [GWAS](../config/coloc-gwas-queries.json), [RNA](../config/coloc-qtl-queries.json), and [reference](../config/coloc-reference-queries.json) records preserve request boundaries, parser/source identities and exact aggregate or ignored reference-subset hashes. The 10,653 GWAS and 165,288 nominal RNA source rows include upstream-tested nonsignificant results and rsID-alias duplicates; transformations and exclusions are separate.

The original `both`-platform-only analysis failed evidence coverage. A [separate source audit](../config/coloc-platform-sources.json) retrieved Ji et al.'s actual publisher [Supplementary Table 1](https://media.springernature.com/original/springer-static/esm/art%3A10.1038%2Fng.3745/MediaObjects/41588_2017_BFng3745_MOESM245_ESM.xlsx) and Table 2. Two failed PMC requests returned HTML, are explicitly excluded from evidence, and are not treated as workbooks. Table 1 establishes 2,170 Omni cases and 9,216 controls; subtracting these from the genome-wide discovery totals gives 701 Affymetrix cases and 2,803 controls. The combined total is 14,890, not the larger selected-SNP replication sample. The [amendment](../config/coloc-platform-plan.json) records its timing after the original coverage failure, before amended posteriors. No original source count or output is silently replaced.

The full, ordinary-gzip published BF archives for BLUEPRINT QTD000031 and DICE QTD000469 were streamed through EOF and hashed in full, retaining only PFKFB3. The [stream record](../config/coloc-published-bf-queries.json) records 2,431,437,676 and 824,792,197 compressed bytes, 41,453,400 and 14,063,519 scanned rows, and 10,552 and 10,264 retained gene rows. These compact complete target-gene vectors include omitted indels in the signal-coverage denominator. Their 95% credible-set PIPs reproduce from the ten component BF vectors within 1.5 × 10⁻¹⁴. Aliases create extra source rows; distinct credible-set sizes are 5 and 1 in BLUEPRINT, and 10 in DICE. The original credible-set table remains unchanged.

GWAS and BLUEPRINT exploratory fits use signed LD from 503 public EUR reference donors; DICE is not fitted using assumed EUR-only LD. Donor-bearing subsets, LD matrices and R fit objects remain ignored. The [R runtime record](../config/coloc-runtime.json) pins R 4.4.3, coloc 5.2.3, susieR 0.14.2 and 124 package artifacts. Original and amended input locks, execution records and reports are separate. The [full report](../reports/colocalisation.md), [summary audit](../reports/coloc-summary.json) and [reproduction guide](coloc-reproduction.md) retain prior sensitivity, platform heterogeneity, external-LD mismatch, missing indels, shared donors and unresolved Treg metadata. No cohort independence, mediation or treatment benefit is inferred.

## Public liver-atlas expression objects

Andrews TS et al. *Single-cell, single-nucleus, and spatial transcriptomics characterization of the immunological landscape in the healthy and PSC human liver*. Journal of Hepatology (2024). DOI: [10.1016/j.jhep.2023.12.023](https://doi.org/10.1016/j.jhep.2023.12.023). The [CELLxGENE collection](https://cellxgene.cziscience.com/collections/0c8a364b-97b5-4cc8-a593-23c38c6f0ac5) distributes the two public objects selected for this phase. The [source manifest](../config/liver-atlas-sources.json) pins the exact collection response, dataset and version UUIDs, retrieval timestamps, byte counts and SHA-256 values. Both files use schema 7.1.0. The collection was revised June 11, 2026; its response was captured September 12. This is the versioned release we measured, not an assumed reconstruction of the paper's analysis subset.

Whole-cell version `ff713455-e6b7-45b3-b2dd-1c9dd26251f6` is 1,118,963,144 bytes with 89,637 observations; nuclear version `34702b40-d0ac-4ec2-8871-776ca012efd0` is 1,543,810,661 bytes with 105,780. Each raw feature table has 32,596 features, with a unique exact match for each of ETS2, PRKD2, UBASH3A, PFKFB3 and BCL2L11. Source gene identifiers, feature flags and original cell/assay annotations remain in the derived audits. The analysis uses `raw/X` counts and all-feature UMI denominators. It makes no DNA-coordinate or individual-genotype inference.

The [metadata configuration](../config/liver-atlas-metadata.json) separately records source facts from the final paper, the explicitly identified preprint supplement, the author's [LiverMap2.0 code at commit cee1e0103eab182f81a676805a53812a7bde9dcf](https://github.com/tallulandrews/LiverMap2.0/tree/cee1e0103eab182f81a676805a53812a7bde9dcf), and the existing 45-row GEO metadata table. The script verifies the collection, H5AD and GEO hashes before metadata parsing; author/document facts are curated inputs, not re-extracted automatically. Retrieval-date precision and local-capture provenance are explicit; unknown exact timestamps are not invented. The 29-row [library summary](../data/derived/liver-atlas-libraries.csv) is at donor/object grain. Title-based donor links are kept distinct from an exact library-UUID crosswalk, which is unavailable.

The two objects share eight donor labels; their union is 10 PSC, eight control and three PBC labels. Reused controls marked `is_primary_data=False` remain included with their provenance flag. No overlapping control-only or subtype objects were added. The nuclear release's larger case counts than the abstract, the author-code `PSC014X` versus release `PSC014` spelling, previous GEO field conflicts and additional chemistry discrepancy remain unresolved in the [metadata audit](../reports/liver-atlas-metadata-audit.json).

The [frozen plan](../config/liver-atlas-plan.json) precedes candidate count inspection and fixes exclusions, donor aggregation, gene set, exact assay matching and 20/50-cell thresholds. All 195,417 observations reconcile in the output ledger; 181,125 remain after the union of exclusions. The [expression audit](../reports/liver-atlas-expression-audit.json) pins analysis code, dependency list and all five numerical outputs. Four aggregate CSVs are versioned; the fifth is an ignored donor-level expression cache. Raw sources retain their originating terms; these transformations do not relicense third-party data. See the [findings](../reports/liver-atlas-cell-context.md) and [reproduction guide](liver-atlas-reproduction.md).

## ETS2 experimental benchmark preparation

Stankey CT et al. (2024), DOI [10.1038/s41586-024-07501-1](https://doi.org/10.1038/s41586-024-07501-1), provides a published experimental mechanism for the next stage. The [separate manifest](../config/ets2-benchmark-sources.json) pins the full-text XML, [Zenodo record 10707942](https://zenodo.org/records/10707942) metadata and its version 1.0 ZIP. Zenodo identifies this release as CC BY 4.0. The 34,636,310-byte archive matches publisher MD5 `9d6b7ddcf377cb194bc72732d807ae80` and SHA-256 `f1da61f2d58b31b1ce1cc418486222a8701698adda76324a10f5f48ecc53239c`. The [inventory](../data/derived/ets2-benchmark-inventory.csv) records 26 selected member hashes without executing source code or opening the four nested spatial-count archives.

Available inputs include source-defined gene sets, disruption/overexpression/MEK-inhibitor rank statistics and R scripts. Exact saved-statistic orientation needs resolution before signed scoring. The article identifies the underlying macrophage RNA experiments as controlled access: EGAD00001011338, EGAD00001011341 and EGAD00001011337. Their expected `raw_counts.txt` inputs are absent from the corresponding public ZIP folders. No controlled-access request, RNA download, program score or treatment simulation occurred. The [next-input assessment](ets2-benchmark-next-inputs.md) separates public summary-level reproduction from analyses requiring those missing donor-level inputs.

## Completed ETS2 program and inhibitor reconstruction

The later [program source manifest](../config/ets2-program-sources.json) pins two publisher XLSX files: Supplementary Tables S1/S2 (5,270,597 bytes, SHA-256 `5390818bf0eb81a027ac145ac65661ed1b6333299d21e257f7d7c3e4b7ad8c74`) and Figure 5 source data (7,907,111 bytes, SHA-256 `b55d3998747dbfd45384f8c11116965029b29c24d8b764f8e4b40811d8594b9a`). Successful retrievals use the publisher's media.springernature.com host; preceding DNS failures at its other static-content host are preserved. Retrieval timestamps describe completed local capture. No failed response was parsed as a workbook.

The [source audit](ets2-source-semantics.md) preserves all six master gene sets, 12,144 S1 rows for three contrasts, 10,196 S2 rows for three contrasts, all five saved symbol-rank vectors, and the public R sample-design labels. Large repeated source-statistic tables are deterministically compressed. Literal `None` symbols, duplicate symbols, missing ranks and the overexpression numeric discrepancies remain explicit. Master lists reproduce S1 at strict adjusted P < 0.05; final publisher tables take precedence for the separate experimental benchmark. R sample pseudonyms describe public experimental design, not reconstructed donor expression.

The [frozen program plan](../config/ets2-program-plan.json) precedes new program scores and experimental concordance summaries, while source definitions and prior five-gene results were already known. It pins the input manifests, previous atlas helper/audit and requirements. Both raw atlas objects were revalidated. The public analysis supplies 18 mapping, 11,036 membership, 360 matching-QC, 144 context, 108 contrast and 80 correlation rows. Full counts, donor scores and random-selection details remain in ignored storage; no personal data or AlphaGenome key is needed.

The subsequent [MEK reconstruction](ets2-inhibitor-reconstruction.md) additionally uses the release's nine-column macrophage gene-set file, archived 500-nM fgsea output and publisher sheet `5j`. Raw ES is recomputed; archived NES is compared to publisher NES. No original count fit, random-set null or P-value calculation is claimed. This strengthens pathway-level provenance while leaving the frozen per-gene treatment-direction exclusion unchanged. Source files retain their original terms and are not relicensed by the repository's analysis code.

## Independent ETS2 validation source preparation

The [perturbation manifest](../config/ets2-validation-perturbation-sources.json) and [context manifest](../config/ets2-validation-context-sources.json) retain primary-source accessions, file routes, discovery limits and readiness exclusions from the September 12 search. A listed file is distinguished from a downloaded and verified file; normalized expression, arrays, expected/fractional counts and integer gene counts are not interchangeable. Individual donor linkage across different studies is unavailable, so separate-publication provenance is not presented as proof of no donor overlap.

Two small context matrices were downloaded into ignored storage. GSE255234's 3,938,694-byte XLSX has SHA-256 `61d23d009b9b9a70a6127e0ae024df18f83fa3ed1a8079b7cbe86854ac5aac18`; GSE193336's 757,738-byte gzip CSV has SHA-256 `2fe82f9b39abd3e4826c74b9280d20fbd3a175d0110b875a35cb84210c7bb98d`. GEO did not advertise SHA-256 checksums for these files; these are local capture hashes, not publisher-supplied checksums. Their source metadata, matrix identifiers and complete numeric-type checks are recorded. Full matrices remain ignored.

The [GSE255234 preparation](../config/ets2-validation-gse255234-preparation.json) pins the SOFT metadata, publication methods, workbook and method/source definitions, with public sample IDs and all planned paired comparisons. It preserves the RefSeq/Ensembl discrepancy, unresolved count-universe provenance and H5 condition metadata; no numerical run is authorized by the manifest's current readiness state. Structural checks read values for validity only, and this broader inspection than the original header-only wording is disclosed. The [readiness report](../reports/ets2-validation-readiness.md) links the exact remaining gates and the prospective amendment.

## GSE84161 original-array and publication sources

The [study source manifest](../config/gse84161-study-sources.json) pins five usable caches: full GSE84161 and GSE84162 SOFT responses, the monocyte CEL archive, PubMed XML and Crossref JSON. The two SOFT responses were captured during the earlier dataset search and reused by exact hash. Their embedded sample tables were inspected for feature IDs and metadata only. Raw and processed expression quantities were not analyzed in this phase. Retrieval timestamp precision and local-versus-publisher checksum provenance are explicit.

[NCBI GEO GSE84161](https://www.ncbi.nlm.nih.gov/geo/query/acc.cgi?acc=GSE84161) distributes `GSE84161_RAW.tar` (146,739,200 bytes). Its local SHA-256 is `6d700c2e2cdcee9ef4eabb654b4c61f5d32952aa6f07bb00d11900f12ec78fff`; no publisher checksum was advertised. All 30 member names match their sample CEL URLs, each gzip stream verifies through EOF, and binary headers identify HG-U133_Plus_2 version-4 CEL files. The [inventory](../data/derived/gse84161-raw-arrays.csv) preserves member-level compressed and decompressed hashes. Full arrays remain ignored.

The GPL570 annotation is embedded in the pinned SOFT snapshot. Its 54,675 rows carry October 6, 2014 annotation dates; the platform record was last updated December 14, 2020. No equivalence to the paper's historical annotation is asserted. The [metadata audit](../reports/gse84161-metadata-audit.json) distinguishes full probe annotations, the 19,944 retained source probes, and unresolved sample-to-donor correspondence. The source VALUE label is retained without assuming that the described heatmap transformations produced the deposited table.

The [publication evidence manifest](../config/gse84161-literature-sources.json) records the original Senger paper, [PMID 28420753](https://pubmed.ncbi.nlm.nih.gov/28420753/), DOI [10.1126/scisignal.aah4273](https://doi.org/10.1126/scisignal.aah4273), and bounded follow-up sources. Publisher challenges and an HTML download named PDF are recorded as unsuccessful retrievals. Indexed primary-paper text on an author-upload host supplies method/caption evidence, without claiming possession of a verified local PDF or supplement. The approximately threefold concentration discrepancy and unknown compound identities are retained. Public access does not relicense any third-party article or data.

The [array preparation manifest](../config/gse84161-array-preparation.json) pins the prospective framework, original program definitions, source manifests and sample/inventory outputs. It enumerates 30 source-labeled samples and all 35 candidate within-block comparisons across seven contrasts. It is not an executable scoring lock; actual normalization software, annotation crosswalk, mapping coverage, anonymous donor correspondence and relevant QC are tracked separately. No absolute score comparison across arrays and RNA sequencing is planned.
